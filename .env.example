# Base URL of the Django backend API (see backend/README.md).
# The backend must be running separately — this frontend never talks to
# Gemini or a database directly.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
